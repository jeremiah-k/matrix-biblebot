matrix-nio[e2e]~=0.25.2
PyYAML~=6.0
python-dotenv~=1.1.0
aiohttp>=3.11.0
rich~=13.7
# asyncio is part of the standard library in Python 3.8+, no need to include it

# Testing and coverage dependencies (dev)
pytest~=8.2
pytest-cov~=4.1
pytest-asyncio~=0.23
pytest-aiohttp~=1.0
coverage~=7.6
