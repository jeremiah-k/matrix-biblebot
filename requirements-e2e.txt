# E2EE (End-to-End Encryption) requirements for matrix-biblebot
# Install with: pip install -r requirements-e2e.txt
# Prefer installing the package extras: pip install ".[e2e]"
# Note: This file mirrors [options.extras_require] e2e in setup.cfg; keep versions in sync.

# Matrix E2EE support
matrix-nio[e2e]>=0.25.2
python-olm>=3.2.15
