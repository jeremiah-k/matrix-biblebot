services:
  biblebot:
    image: ghcr.io/jeremiah-k/matrix-biblebot:latest
    container_name: matrix-biblebot
    restart: unless-stopped
    stop_grace_period: 15s
    # If you run plain `docker compose`, set UID/GID via `.env` or exported env vars.
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - BIBLEBOT_HOME=/data
      - TZ=${TZ:-UTC}
    volumes:
      - ${BIBLEBOT_HOST_HOME:-$HOME/.config/matrix-biblebot}:/data
